1.  Yes, you can definitely make a temp directory. To do this you need to type `mkdir temp` into the command line.<p>

2.  Yes. If you are already in the class directory (type `pwd` to find out), you would type `mkdir log` into the command line. 
